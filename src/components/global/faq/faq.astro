---
import MarkdownIt from "markdown-it";
const md = new MarkdownIt({ html: true });

// Define TypeScript interfaces
interface FaqItemData {
  title: string;
  description: string;
}

interface Props {
  title: string;
  title_suffix: string;
  description: string;
  FAQ: FaqItemData[];
}

const { title_suffix, description, FAQ } = Astro.props;
---

<section class="py-16 xl:py-24 2xl:py-32 bg-primary/10">
  <!-- <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-center">
      <div class="w-full lg:w-2/3 text-center">
        <div class="mb-12 xl:mb-16">
          <Heading level={2} class="mb-6 tracking-tight">
            <editable-text data-prop="title">{title}</editable-text>{" "}
            <span class="text-primary">
              <editable-text data-prop="title_suffix">
                {title_suffix}
              </editable-text>
            </span>
          </Heading>
          <div class="text-lg text-gray-600 leading-relaxed" data-editable="text" data-prop="description" set:html={md.render(description)} />
        </div>
      </div>
    </div>
    <div class="flex justify-center">
      <div class="w-full">
        <div class="space-y-4 grid gap-3" data-editable="array" data-prop="FAQ">
          {
            FAQ &&
              FAQ.map((q: FaqItemData, i: number) => (
                <editable-array-item data-prop="FAQ">
                  <FaqItem faq={q} i={i} />
                </editable-array-item>
              ))
          }
        </div>
      </div>
    </div>
  </div> -->
  <div
    class="mt-7 max-w-7xl mx-auto p-4 space-y-4 grid gap-3 bg-white"
    data-editable="array"
    data-prop="FAQ"
  >
    <template>
      <p data-editable="text" data-prop="title">Nothing here yet</p>
    </template>
    {
      FAQ?.length > 0 &&
        FAQ.map((q: FaqItemData) => (
          <editable-array-item>
            <p data-editable="text" data-prop="title">
              {q.title}
            </p>
          </editable-array-item>
        ))
    }
  </div>
</section>
